// This returns the minimum of two integer. Supposedly valid.
// min(x:int, y:int | z:int) {
//  z := x ;
//  assert ((z = x) || (z = y))
// }

test(x:int | x:int) {
  assume (x > 5);

  if (x > 5) then {
    if (x < 3) then {
      skip
    } else {
      skip
    }
  } else {
    skip
  };

  assert (x > 5)
}
