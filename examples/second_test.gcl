// This returns the minimum of two integer. Supposedly valid.
// min(x:int, y:int | z:int) {
//  z := x ;
//  assert ((z = x) || (z = y))
// }

test(x:int | x:int) {
  assume (x >= 0);
  
  if (x > 0) then {
    x := x + 1
  } else {
    x := x - 1
  };
  
  assert (x >= 1)
}
